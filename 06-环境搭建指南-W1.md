# 06 · W1 环境搭建指南（Windows 一步步来）

> 目标：半天到一天搭完，让"学习环境"可用。全程照着做，做完打勾。
> 以下命令在 **PowerShell** 里运行（win 键 → 输入 powershell → 回车）。

## 第 1 步：安装 WSL2 + Ubuntu（约 20 分钟）

1. 以管理员身份打开 PowerShell，运行：
```powershell
wsl --install
```
2. 安装完重启电脑，首次启动会要求设置 Linux 用户名和密码（记住它们）。
3. 验证：
```powershell
wsl -l -v
```
   看到 Ubuntu 且 VERSION 是 2 即可。
4. 打开 Ubuntu 终端（开始菜单搜 Ubuntu），以后**代码相关操作都在 Ubuntu 里做**。

> 如果 `wsl --install` 报错，检查：BIOS 开启虚拟化；Windows 版本较旧则手动装 WSL2（搜索"WSL2 手动安装"）。

## 第 2 步：更新 Ubuntu 并安装 Python（约 10 分钟）

在 Ubuntu 终端里依次运行：
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y python3 python3-pip python3-venv git curl
python3 --version   # 看到 3.10+ 即可（3.11/3.12 更好）
```

## 第 3 步：安装 VS Code（Windows 端，约 5 分钟）

1. 去 https://code.visualstudio.com 下载安装。
2. 打开 VS Code，左侧扩展商店搜索并安装：
   - **WSL**（微软官方）→ 装完后点左下角绿色图标可"在 WSL 中打开"
   - **Python**（微软官方）
   - **GitHub Copilot**（若你有 GitHub 学生包或订阅；没有就用 Codex 或先跳过）
3. 在 Ubuntu 里进入你的工作目录并打开：
```bash
mkdir -p ~/learn && cd ~/learn
code .
```

## 第 4 步：Git + GitHub（约 15 分钟）

1. 注册 GitHub 账号：https://github.com （用户名建议：姓名拼音+数字）
2. Ubuntu 里配置：
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```
3. 生成 SSH 密钥并添加（免密推送）：
```bash
ssh-keygen -t ed25519 -C "你的邮箱"   # 一路回车
cat ~/.ssh/id_ed25519.pub
```
4. 复制输出内容 → GitHub 右上角头像 → Settings → SSH and GPG keys → New SSH key → 粘贴保存。
5. 验证：`ssh -T git@github.com` 看到 "Hi 用户名!" 即成功。

## 第 5 步：创建你的"学习仓库"（约 15 分钟）

1. GitHub 网页右上角 New repository → 名字 `learn-ai` → Public → Create。
2. Ubuntu 里：
```bash
cd ~/learn
git clone git@github.com:你的用户名/learn-ai.git
cd learn-ai
echo "# learn-ai" > README.md
echo "## 我的 AI 转行学习仓库" >> README.md
git add . && git commit -m "init" && git push
```
3. 以后每天的代码、笔记、复盘都往这个仓库里放。

## 第 6 步：Python 虚拟环境习惯（约 5 分钟）

以后每个项目都建独立环境：
```bash
cd ~/learn/learn-ai
python3 -m venv .venv
source .venv/bin/activate   # 每次进入项目先激活
pip install numpy pandas
```

## 第 7 步：AutoDL 云 GPU（约 15 分钟）

> 阶段 2 之后才真正需要 GPU，但账号先注册好。

1. 注册 https://www.autodl.com （手机号 + 实名认证）。
2. 充值小额（¥50 即可，按小时扣费，用完关停几乎不花钱）。
3. 先不租卡，W17 再按教程租。

## 第 8 步：Ollama 本地跑通第一个模型（约 15 分钟）

1. 下载 Ollama（Windows 版）：https://ollama.com/download
2. 安装后在 PowerShell 运行：
```powershell
ollama run qwen2.5:7b
```
3. 等它下载（约 4-5GB），然后直接输入对话，比如：
```
你好，介绍一下你自己
```
4. 成功对话 = 你完成了"人生第一次跑大模型"。截图留念。
5. 退出：输入 `/bye`。

## 第 9 步：配好 AI 编程工具（约 10 分钟）

- **GitHub Copilot**：VS Code 扩展安装 → 登录 GitHub → 免费试用/学生包。
- 或者用 **Codex**（你已经在用）：把日常"卡住的问题"丢给它讲解思路。
- 规则：**让 AI 讲思路，你自己写代码**，不要整段复制。

## W1 完成检查单
- [ ] WSL2 + Ubuntu 可打开
- [ ] Python 3.10+ 可用
- [ ] VS Code 能在 WSL 里打开
- [ ] Git 配置完成，能 push 到 GitHub
- [ ] 学习仓库 learn-ai 已建好
- [ ] AutoDL 注册并充值
- [ ] Ollama 跑通 qwen2.5:7b 对话
- [ ] AI 编程工具可用
